{
  "name": "analytics-exporter",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "export": "pnpm build && pnpm start"
  },
  "dependencies": {
    "@clickhouse/client": "^1.23.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
